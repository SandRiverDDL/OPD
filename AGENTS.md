# OPD 工作规范

本文件只保留通用工作原则、操作边界和当前仓库必要约束。不要把某次任务的详细过程、临时命令或完整报告堆进来。

## 1. 工作原则

### Think Before Coding

- 不要替用户做未经确认的假设。
- 如果需求有歧义，先说明理解，再提问或给出选项。
- 如果存在明显更简单的方案，应主动指出，不要默认走复杂路径。
- 如果信息不足以安全推进，先停下来澄清。
- 概念性或配置性问题先给通用答案；只有确实需要排查当前仓库状态时，才扫描源码和环境。

### Simplicity First

- 只做当前任务真正需要的最小实现或最小改动。
- 不为了“未来可能会用到”增加抽象、配置、模板或脚本。
- 不额外扩展任务边界，不主动制造后续维护负担。

### Surgical Changes

- 只修改与当前任务直接相关的文件和内容。
- 不顺手重写相邻文档、格式、注释、命名或目录结构。
- 保留用户已有修改，不覆盖或回滚与当前任务无关的内容。
- 发现无关问题可以报告，但不要自行扩改。

### Goal-Driven Execution

- 多步骤任务先给简短计划，再执行。
- 尽量把任务转换为可验证结果。
- 完成后说明实际修改、实际验证和未验证内容。
- 不把“命令已启动”写成“任务已完成”。

## 2. 执行规范

- 预计超过 1 分钟的任务应后台运行，不要长期占用前台会话。
- 后台任务应记录 PID 或调度系统 job ID、日志路径以及产物路径；结束后检查退出状态、日志尾部和实际产物。
- 短 smoke test、单元测试和小范围检查可以前台运行。
- 训练、评测和数据处理日志统一写入仓库根目录 `logs/` 或其任务子目录；`/tmp/` 只用于短生命周期临时文件。
- 大文件和长日志使用 `head`、`tail`、`rg`、抽样和分块处理，不要直接完整读入上下文。
- 多个关键词尽量合并为一次有范围的查询；对话中只保留代表性错误、成功/失败计数和结论。
- 已知少量目标文件时，只检查目标文件及其直接父目录；不要为处理局部问题扫描整个仓库或无关数据目录。
- CephFS 等共享文件系统上避免无必要的全量 `find`、`du`、哈希计算或大规模元数据扫描。

## 3. Git 边界

- 不在非 Git 任务中例行执行全仓 `git status`、`git diff` 或子模块扫描；确有需要时优先检查本次目标路径。
- 提交时只显式暂存本次文件或 hunk；禁止使用 `git add .` 和 `git add -A`。
- 不提交日志、checkpoint、模型、数据、密钥、敏感运行时配置或用户已有的无关修改。
- 不自动 push、merge、rebase、reset、clean、tag 或改写历史。
- 未经用户要求，不因“顺手整理”创建额外提交。

## 4. 当前仓库

主要目录和入口：

```text
README.md                   项目说明、环境配置和训练/验证入口
on_policy_distillation.sh   OPD 训练入口
grpo.sh                     GRPO 训练入口
verl/                       训练框架代码
LlamaFactory/               SFT 训练依赖
scripts/                    推理、数据处理和验证脚本
datasets/                   项目数据
verl_example/               verl 启动示例
docs/STATE.md               当前仓库稳定状态
```

- 环境安装、训练和验证方式以 `README.md` 为首要参考。
- 修改 `verl/` 或 `LlamaFactory/` 前，先确认问题确实位于依赖代码，而不是启动脚本、配置、数据或环境。
- 子目录若存在更具体的 `AGENTS.md`，处理该目录内容时以更具体的规范为准。

## 5. 实验、模型和数据

- 不要仅根据模型名推断 checkpoint 格式、tokenizer、chat template 或训练后端；应检查实际文件和生效配置。
- 先查看数据 schema 和少量样本，再执行全量处理。
- 文件超过 100 MB 时，禁止把完整内容读入上下文。
- 对大规模 JSONL、Parquet、日志或 checkpoint，优先使用元数据、抽样和流式处理。
- 运行训练或评测前，核对模型路径、数据路径、GPU/节点资源、输出目录和断点恢复设置。
- 运行时行为、分布式调用和 worker 环境不能只凭静态代码推断，应结合实际日志和产物验证。

### 实验配置不可擅自修改

- **MUST NOT** 擅自修改任何会影响实验结论的配置，包括模型、数据划分、长度限制、采样参数、batch size、学习率、训练步数、验证时机、reward、并行策略和 teacher 参数。
- **MUST** 先读取当前启动脚本、配置文件和已有运行记录，确认用户要求的变更范围。
- **MUST** 在提交训练或评测任务前核对最终生效的关键配置。
- 未在用户要求中明确变更的配置，不得为了方便启动、资源估算或调试而静默修改。
- 如果用户明确要求自主修复问题，且必须调整实验配置才能验证，应说明修改原因、字段、原值、新值和验证目的。

## 6. 代码与配置习惯

- 代码按职责组织；CLI 和启动脚本保持轻量，避免职责混杂的超大脚本，也不要机械拆分或过度抽象。
- 配置问题先看实际配置和启动命令，再判断默认值。
- 使用表达实际含义的变量名和函数名，避免难以理解的缩写。
- 不添加无声 fallback；必要的 fallback 必须有明确日志或错误信息。
- 发现数据字段、模型路径、tokenizer 或配置不一致时，应尽早 fail fast。
- 修复问题时优先添加能够复现缺陷的最小验证；无法运行完整验证时，明确说明原因。

## 7. 状态与记录

- `docs/STATE.md` 只记录当前仓库可复用的稳定状态，不堆放完整日志、重复 traceback 或一次性排查过程。
- 长任务中断、环境切换或上下文即将压缩时，记录简短状态摘要：任务、命令或 job、日志、配置、已完成阶段、异常和下一步。
- 实验报告应写入对应任务目录；大日志保留路径和关键计数即可。
- 更新稳定状态时同步维护 `docs/STATE.md`，删除已经失效的信息，避免把猜测写成事实。

## 8. 工作语言与汇报

- 与用户交流默认使用中文。
- 代码、变量名、命令和技术标识保留英文。
- 回答和任务汇报结论先行，默认简洁；详细过程只在必要或用户要求时展开。
- 不虚构项目进展、测试结果、训练 step、checkpoint 或评估效果。
- 未实际检查的内容明确写“未验证”。

## 9. 高风险边界

- 不在仓库外随意写代码和文档。
- 不主动生成当前任务不需要的脚本、模板或报告。
- 发现可能泄露 token、API key、secret 或敏感运行时配置的文件时，禁止打印、复制和提交。
- 执行删除、覆盖、迁移、大规模数据改写或可能影响运行中任务的操作前，必须确认范围和恢复方式。
